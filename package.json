{
  "name": "primeiro",
  "version": "1.0.0",
  "description": "Primeiro Repositório no GitHub",
  "main": "index.js",
  "scripts": {
    "dev": "npm run services:up && next dev",
    "dev:watch": "npm run services:up && nodemon --exec \"next dev\" --watch .",
    "services:up": "docker compose -f infra/compose.yaml up -d",
    "services:down": "docker compose -f infra/compose.yaml down",
    "services:stop": "docker compose -f infra/compose.yaml stop",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "next": "^13.1.6",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "nodemon": "^3.1.11",
    "prettier": "^3.6.2"
  }
}
